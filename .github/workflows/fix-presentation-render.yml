name: Fix presentation render

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fix-presentation-render.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Patch Streamlit presentation HTML rendering
        run: |
         